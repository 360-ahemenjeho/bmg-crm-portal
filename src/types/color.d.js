import {
  backgroundColors,
  foregroundColors,
  borderColors,
  scrollBarColors,
  buttonColors,
  inputColors,
  mainColors,
  menuColors,
  menuItemColors,
  statusColors,
} from "@/constants/theme";

/**
 * @typedef {Object} ColorContextValue
 * @property {"light" | "dark"} theme
 * @property {() => void} toggleTheme
 * @property {typeof backgroundColors["light"]} bg
 * @property {typeof foregroundColors["light"]} fg
 * @property {typeof borderColors["light"]} border
 * @property {typeof scrollBarColors["light"]} scrollbar
 * @property {typeof buttonColors["light"]} button
 * @property {typeof inputColors["light"]} input
 * @property {typeof mainColors["light"]} main
 * @property {typeof menuColors["light"]} menu
 * @property {typeof menuItemColors["light"]} menuItem
 * @property {typeof statusColors["light"]} status
 */

export {};
